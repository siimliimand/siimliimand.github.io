---
schema: girder.openspec/v1
title: Redesign GitHub Pages landing with semantic markup, design tokens, responsive layout, and an optimized hero image
intent: Visitors to the page get a layout that works on phones and desktops, stays readable in dark mode, passes WCAG2AA automated checks, and loads a hero image that is no longer an unoptimized original JPEG.
tasks:
  - id: semantic-restructure
    title: Restructure index.html into semantic landmarks with an external stylesheet
    type: refactor
    scope_globs: ["index.html", "css/**"]
    success_criteria:
      - "index.html wraps all content in header, nav, main, and footer landmarks (verify: grep -c '<main' index.html returns exactly 1)"
      - "index.html links css/styles.css and contains no <style> blocks or style= attributes (verify: grep -ciE 'style=' index.html returns 0)"
      - "css/styles.css exists and `npx html-validate index.html` exits 0"
    depends_on: []
  - id: design-tokens
    title: Define color, spacing, and font tokens with a dark-mode palette in styles.css
    type: code_change
    scope_globs: ["css/styles.css"]
    success_criteria:
      - "css/styles.css defines :root custom properties --color-bg, --color-text, --color-accent, --space-1 through --space-4, and --font-body, and body/heading rules reference them via var() (verify: grep -c 'var(--' css/styles.css returns at least 10)"
      - "css/styles.css contains a @media (prefers-color-scheme: dark) block that overrides --color-bg, --color-text, and --color-accent"
      - "`npx lighthouse http://localhost:8080 --only-categories=accessibility --output=json --output-path=./lighthouse.json` reports an accessibility score of at least 0.95 for body-text contrast in both light and dark emulation"
    depends_on: [semantic-restructure]
  - id: responsive-layout
    title: Add responsive breakpoints with a horizontal-overflow regression test
    type: code_change
    scope_globs: ["css/styles.css", "test/responsive.spec.js"]
    success_criteria:
      - "css/styles.css contains @media queries at 480px and 900px that collapse the page container to a single column (verify: grep -c '@media' css/styles.css returns at least 2)"
      - "test/responsive.spec.js asserts document.documentElement.scrollWidth <= 375 at a 375px viewport and `npx playwright test test/responsive.spec.js` exits 0 against a server on port 8080"
    depends_on: [semantic-restructure, design-tokens]
  - id: accessibility-markup
    title: Fix accessibility gaps in page markup
    type: fix
    scope_globs: ["index.html"]
    success_criteria:
      - "the <img> element for inkscape-image-editor-for-linux_orig.jpg carries a descriptive alt attribute and the html element carries a lang attribute (verify: grep -E 'alt=\"[^\"]{4,}\"' index.html and grep -E '<html[^>]*lang=' index.html both match)"
      - "`npx pa11y http://localhost:8080 --standard WCAG2AA --reporter json` reports 0 issues"
    depends_on: [semantic-restructure]
  - id: optimize-hero-image
    title: Generate a resized hero image and serve it instead of the original JPEG
    type: code_change
    scope_globs: ["assets/**", "index.html"]
    success_criteria:
      - "assets/inkscape-screenshot-1200.jpg exists with width <= 1200px and size <= 250KB (verify: `magick identify -format '%w %b' assets/inkscape-screenshot-1200.jpg`)"
      - "index.html references assets/inkscape-screenshot-1200.jpg and no longer references inkscape-image-editor-for-linux_orig.jpg (verify: grep -c 'inkscape-image-editor-for-linux_orig.jpg' index.html returns 0)"
    depends_on: [semantic-restructure]
  - id: design-doc
    title: Document the design system tokens and breakpoints in docs/design.md
    type: documentation
    scope_globs: ["docs/design.md"]
    success_criteria:
      - "docs/design.md exists and lists every custom property defined in css/styles.css with its light and dark values (verify: each :root property name from css/styles.css appears in docs/design.md)"
      - "docs/design.md documents the 480px and 900px breakpoints and links a before/after screenshot pair stored under docs/design-assets/"
    depends_on: [design-tokens, responsive-layout]
---

The page currently is a single root-level index.html plus an unoptimized source screenshot, so the redesign is split so each task lands independently: first the markup is normalized into semantic landmarks with a real stylesheet, then the visual system (tokens, dark mode, responsive grid) is layered on, then the accessibility and image-weight fixes that depend on the new markup, and finally a design document under docs/ so future edits have a reference. Verification uses html-validate, Lighthouse, pa11y, Playwright, and ImageMagick against a local server (e.g. `npx http-server -p 8080`); the original `inkscape-image-editor-for-linux_orig.jpg` is kept in the repo as the source asset for regeneration.