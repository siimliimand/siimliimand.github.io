---
schema: girder.openspec/v1
title: "Convert landing page into Siim Liimand's single-page CV"
intent: "The existing landing page (index.html, hero.svg, styles.css) becomes a complete, print-ready CV for Siim Liimand: a full-stack web freelancer with 15+ years of experience building websites, portals, and e-commerce shops. Visitors see structured sections for contact, summary, skills, experience, and education; the skills section covers HTML, CSS, JavaScript, PHP, Python, Node.js, AI-assisted development, Linux server administration (Nginx/Apache, DNS, SSL/TLS), GitHub Actions CI/CD, Docker, Git, MySQL/PostgreSQL, WordPress, REST API integration, performance optimization, SEO, WCAG accessibility, security hardening, and backups/monitoring. The page passes HTML validation and an automated check script, and exports cleanly to PDF via the browser print dialog."
tasks:
  - id: cv-content-draft
    title: "Draft CV copy and skill inventory in docs/cv-content.md"
    type: documentation
    scope_globs: ["docs/cv-content.md"]
    success_criteria:
      - "docs/cv-content.md exists and contains the headings: Contact, Summary, Skills, Experience, Selected Projects, Education"
      - "docs/cv-content.md states 15+ years of freelance experience building websites, portals, and e-commerce shops"
      - "docs/cv-content.md lists HTML, CSS, JavaScript, PHP, Python, and Node.js and names AI-assisted development as an explicit working practice"
      - "docs/cv-content.md lists the extended skill set: Linux server administration (Nginx/Apache, DNS, SSL/TLS via Let's Encrypt), GitHub Actions CI/CD, Docker, Git, MySQL/PostgreSQL, WordPress, REST API integration, performance optimization, SEO, WCAG 2.1 accessibility, security hardening, and backups/monitoring"
      - "Experience and Selected Projects contain at least 3 entries each, every entry carrying a role, a tech stack, and a year range"
      - "running `grep -E 'TODO|TBD|Lorem' docs/cv-content.md` produces no output"
    depends_on: []
  - id: cv-html
    title: "Rewrite index.html as the semantic CV document"
    type: code_change
    scope_globs: ["index.html"]
    success_criteria:
      - "index.html sets lang=\"en\", the title element text 'Siim Liimand — Full-Stack Web Developer CV', one meta description, and Open Graph title/description meta tags (verifiable with grep on index.html)"
      - "index.html contains section elements with ids contact, summary, skills, experience, and education, in that document order"
      - "the contact section in index.html contains a mailto: link and a GitHub profile link"
      - "index.html links styles.css and references hero.svg and assets/favicon.svg"
      - "the skills section in index.html groups items under the headings Languages, AI-Assisted Development, Servers and DevOps, and Web Platforms, matching docs/cv-content.md"
      - "`npx html-validate index.html` exits with status 0"
    depends_on: [cv-content-draft]
  - id: cv-hero-favicon
    title: "Rebrand hero.svg and add assets/favicon.svg"
    type: code_change
    scope_globs: ["hero.svg", "assets/favicon.svg"]
    success_criteria:
      - "hero.svg declares a viewBox attribute and contains the visible text 'Siim Liimand' and the tagline 'Full-Stack Web Freelancer'"
      - "hero.svg uses only inline fills and text (running `grep -E 'xlink:href|<image|http' hero.svg` matches nothing)"
      - "assets/favicon.svg exists and its root element is svg"
    depends_on: [cv-content-draft]
  - id: cv-styles
    title: "Restyle styles.css for responsive and print-ready CV layout"
    type: code_change
    scope_globs: ["styles.css", "css/**"]
    success_criteria:
      - "styles.css declares the palette as CSS custom properties in a :root block, including --color-text and --color-background as hex values whose WCAG contrast ratio is at least 4.5:1"
      - "styles.css contains an @media print block with an @page rule declaring margins and a body background of transparent"
      - "styles.css contains an @media (max-width: 600px) block that lays out the main column at 100% width"
      - "styles.css contains no !important declarations (running `grep -c '!important' styles.css` returns 0)"
      - "styles.css contains no TODO or Lorem markers (running `grep -E 'TODO|Lorem' styles.css` produces no output)"
    depends_on: [cv-html]
  - id: cv-tests
    title: "Add dependency-free validation script test/validate-cv.js"
    type: test_change
    scope_globs: ["test/validate-cv.js"]
    success_criteria:
      - "test/validate-cv.js uses only fs and built-in modules, and running `node test/validate-cv.js` from the repository root exits with status 0"
      - "test/validate-cv.js asserts on index.html: the page title, the five section ids (contact, summary, skills, experience, education), the mailto link, and the assets/favicon.svg reference; each failed assertion prints a message naming the missing item and the script exits 1"
      - "test/validate-cv.js asserts on styles.css: an @media print block, an @media (max-width: 600px) block, and zero TODO/Lorem markers, exiting 1 on any failure"
      - "test/validate-cv.js parses the hex values assigned to --color-text and --color-background in styles.css, computes their WCAG contrast ratio, and exits 1 when the ratio is below 4.5"
      - "temporarily removing the mailto link from index.html makes `node test/validate-cv.js` exit with status 1, and restoring it returns the exit status to 0"
    depends_on: [cv-html, cv-styles, cv-hero-favicon]
  - id: cv-docs
    title: "Write maintenance guide docs/cv-maintenance.md"
    type: documentation
    scope_globs: ["docs/cv-maintenance.md"]
    success_criteria:
      - "docs/cv-maintenance.md exists and maps each CV section (contact, summary, skills, experience, education) to its location in index.html by section id"
      - "docs/cv-maintenance.md documents the commands `node test/validate-cv.js` and `npx html-validate index.html` with their expected exit codes"
      - "docs/cv-maintenance.md includes a numbered procedure for exporting the CV to PDF via the browser print dialog, noting that the @media print block applies automatically"
    depends_on: [cv-html, cv-styles]
---

The change is split so content, markup, visual design, assets, verification, and documentation each land as an independent, reviewable unit. The copy is drafted first because both the markup and the hero tagline derive from it; the validator depends on all production files so it can assert the finished state end to end.

The "and a lot more" in the request is resolved into a concrete, recruiter-scannable inventory: Linux server administration (Nginx/Apache, DNS, SSL/TLS via Let's Encrypt), GitHub Actions CI/CD, Docker, Git workflows, MySQL/PostgreSQL, WordPress, REST API integration, performance optimization, SEO, WCAG 2.1 accessibility, security hardening, and backups/monitoring. If Siim wants items added or removed, `docs/cv-content.md` is the single source of truth to edit.

Two notes for the implementer: contact details (email address, GitHub username, phone) must be supplied with real values during `cv-html` since none were provided in the request, and the print stylesheet is what makes "export to PDF" a one-keystroke operation — the `@page` rule and transparent backgrounds exist specifically for that path.