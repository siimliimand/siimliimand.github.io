---
schema: girder.openspec/v1
title: Restructure index.html with semantic markup, a responsive stylesheet, and a gradient SVG hero image
intent: Visitors get a page built from semantic HTML5 with a linked responsive stylesheet (including dark-mode support) and a scalable vector hero graphic; the legacy inkscape JPEG is removed from the repository.
tasks:
  - id: semantic-html
    title: Restructure index.html with semantic elements, viewport meta, and image alt text
    type: code_change
    scope_globs: ["index.html"]
    success_criteria:
      - "index.html contains exactly one <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"> tag inside <head>"
      - "index.html wraps the page content in <header>, <main>, and <footer> elements, each present at least once"
      - "grep -iE '<(font|center|marquee|blink)' index.html returns no matches"
      - "the <img> element in index.html carries a non-empty alt attribute"
    depends_on: []
  - id: stylesheet
    title: Add styles.css with responsive layout and dark-mode support and link it from index.html
    type: code_change
    scope_globs: ["index.html", "styles.css"]
    success_criteria:
      - "styles.css exists at the repository root and index.html contains <link rel=\"stylesheet\" href=\"styles.css\"> inside <head>"
      - "styles.css defines at least three custom properties on the :root selector and references them with var() at least twice"
      - "styles.css contains at least one layout rule using display: grid or display: flex"
      - "styles.css contains at least one @media block with a min-width or max-width breakpoint and at least one @media (prefers-color-scheme: dark) block that overrides declared colors or custom properties"
    depends_on: ["semantic-html"]
  - id: svg-hero-image
    title: Replace the JPEG with a gradient hero.svg and delete the old asset
    type: code_change
    scope_globs: ["index.html", "hero.svg", "inkscape-image-editor-for-linux_orig.jpg"]
    success_criteria:
      - "hero.svg exists at the repository root and xmllint --noout hero.svg exits 0"
      - "the root <svg> element in hero.svg declares viewBox, width, and height attributes"
      - "hero.svg contains at least one <linearGradient> or <radialGradient> element with at least two <stop> children"
      - "index.html references hero.svg via an <img src=\"hero.svg\"> element and grep -i inkscape index.html returns no matches"
      - "after running git rm inkscape-image-editor-for-linux_orig.jpg, git ls-files -- inkscape-image-editor-for-linux_orig.jpg prints nothing"
    depends_on: ["stylesheet"]
---

The request to "modernize" the page and use "something cooler" imagery was resolved into three concrete, verifiable work items against the current tree (`index.html` plus the single JPEG asset):

1. **semantic-html** replaces any legacy markup in `index.html` with semantic HTML5 structure, a viewport meta tag, and accessible alt text — the baseline the other tasks build on.
2. **stylesheet** introduces a root-level `styles.css` linked from `index.html`, defined by measurable features (custom properties, grid/flex layout, a responsive breakpoint, and a `prefers-color-scheme: dark` block) so "modern styling" is checkable rather than subjective.
3. **svg-hero-image** swaps the raster JPEG for a hand-authored `hero.svg` using a multi-stop gradient as the concrete stand-in for "cooler," validates it as well-formed XML, and removes the old `inkscape-image-editor-for-linux_orig.jpg` so no unreferenced asset remains.

Tasks are chained (`semantic-html` → `stylesheet` → `svg-hero-image`) because all three edit `index.html`; the ordering keeps the single-file edits conflict-free and each task is small enough to finish within one orchestrator turn.